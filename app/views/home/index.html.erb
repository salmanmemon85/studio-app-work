<div class="flex flex-row min-h-screen bg-gray-100 text-gray-800">
 <%= render 'shared/sidebar' %>
  
    <main class="main flex flex-col flex-grow -ml-64 md:ml-0 transition-all duration-150 ease-in">
        <%= render 'shared/header' %>
      <div class="main-content flex flex-col flex-grow p-4">
        <h1 class="font-bold text-2xl text-gray-700">Dashboard</h1>
        <div
          class="flex flex-col flex-grow  rounded mt-4"
        >
        <div class="container mx-auto">
            <div class="grid grid-cols-12 gap-4">
                <div class="col-span-3">
                <div class="bg-white w-full h-24 p-4 ">
                <h4 class="font-bold text-xs text-gray-700">Recent followers</h4>
                <h4 class="font-bold text-xs text-gray-400">Today</h4>
                </div>
                </div>
                <div class="col-span-4">hello</div>
                <div class="col-span-5">hello</div>

                </div>
        </div></div>
      </div>
        <%= render 'shared/footer' %>
     
    </main>
  </div>